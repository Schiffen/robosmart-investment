streamlit==1.60.0
yfinance==1.5.1
pandas==2.2.3
numpy==2.1.3
plotly==5.24.1
anthropic==0.118.0
python-dotenv==1.0.1

# --- PDF export (report.py, report_charts.py) -----------------------------
# All three install everywhere, so the export is NOT conditional on the
# environment. reportlab and svglib build the document and embed the marks as
# vectors; matplotlib draws the figures.
#
# NOT kaleido, which is the obvious choice for rasterising a Plotly figure and
# fails in three different ways: 0.2.1 ships no macOS arm64 binary, 1.x refuses
# Plotly 5.24 through fig.to_image(), and 1.x drives a real headless Chrome
# that Streamlit Community Cloud does not have. That last one left the DEPLOYED
# export with tables and no charts, which is not an export. matplotlib needs no
# browser, ships manylinux wheels, and renders all five figures in ~0.5s
# against kaleido's ~7s. See report_charts.py.
matplotlib>=3.8
reportlab>=4.0
svglib>=1.5
